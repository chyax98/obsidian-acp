# Changelog

所有重要更改将记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

## [0.2.0] - 2025-12-22

### 修复
- **权限响应格式**：修复 ACP 权限响应格式，从 `{ outcome: { type: 'selected' } }` 改为正确的 `{ outcome: { outcome: 'selected' } }`（参考 AionUI 实现）
- **历史消息覆盖**：修复 `MessageRenderer.renderMessage()` 中 `container.empty()` 导致新消息覆盖历史消息的问题
- **类型安全**：修复 `PermissionOutcome` 联合类型处理，正确处理 `cancelled` 状态

### 优化
- **布局紧凑化**：减少消息间距、内边距，使界面更紧凑
- **工具显示**：为不同工具类型添加 emoji 图标（🔧 Bash、📖 读取、✏️ 写入等）
- **状态颜色**：为工具调用状态添加颜色区分（pending/蓝、executing/橙、completed/绿、error/红）

### 新增
- **AionUI 参考文档**：创建 `docs/aionui-reference.md` 记录关键实现模式
- **流式消息缓冲**：实现 `StreamingMessageBuffer` 类，减少 UI 更新频率
- **模式更新事件**：实现 `current_mode_update` 和 `available_commands_update` 处理
- **思考过程渲染**：实现 `agent_thought_chunk` 处理和 UI 渲染

## [0.1.0] - 2024-12-18

### 初始版本
- ACP 协议基础实现
- Claude Code / Kimi / Codex 后端支持
- 基础聊天界面
- 权限请求弹窗
- 会话持久化
